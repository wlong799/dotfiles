# Configuration file for sxhkd, a hotkey daemon


## PROGRAM HOTKEYS 

# start urxvt (terminal)
super + Return
    urxvt

# start dmenu_run (program launcher)
super + space
    dmenu_run -h $PANEL_HEIGHT -fn "$PANEL_FONT"

# start dmenu_pass (password manager) (PLACEHOLDER)
super + shift + space
    dmenu_run -h $PANEL_HEIGHT -fn "$PANEL_FONT"

## SYSTEM HOTKEYS

# lock computer or logout
super + {_,shift +} Escape
    {~/.screenlock, bspc quit}

# control master volume
XF86Audio{LowerVolume,RaiseVolume}
    amixer sset Master 5%{-,+}

XF86AudioMute
    amixer sset Master toggle

# control screen brightness
XF86MonBrightness{Down,Up}
    xbacklight -{dec,inc} 10

## BSPWM HOTKEYS

# close (or kill) the current node
super + {_,shift + }q
    bspc node -{c,k}

# focus on node with click
~button1
    bspc pointer -g focus

# focus the next/previous desktop
super + {Tab,grave}
    bspc desktop -f {next, prev}

# focus the next/previous node
alt + {Tab,grave}
    bspc node -f {next,prev}

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}'

# focus/swap the node in the given direction
super + {_,shift + }{h,j,k,l}
    bspc node -{f,s} {west,south,north,east}
